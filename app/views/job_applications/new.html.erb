<div class="container mx-auto px-4">
  <h1 class="text-2xl font-bold mb-4">Apply for <%= @job.title %></h1>

  <%= render 'form', job_application: @job_application %>

  <%= link_to 'Back', job_path(@job), class: "bg-gray-500 bg-gray-700 text-white font-bold py-2 px-4 rounded mt-4 inline-block" %>
</div>
